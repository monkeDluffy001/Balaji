import { Box, Typography } from '@mui/material';

const ContractConfiguration = () => {
  return (
    <Box>
      <Typography>Contract configuration page</Typography>
    </Box>
  );
};

export default ContractConfiguration;
